---
import BackToTop from "@components/ui/BackToTop";
import Breadcrumbs from "@components/ui/Breadcrumbs";
import Layout from "@layouts/Layout.astro";
import { marked } from "marked";
import { siteConfig } from "../config";

const termsContent = Object.values(import.meta.glob("./content/terms.md", { eager: true }))[0] as { rawContent(): string };
const htmlContent = marked(termsContent.rawContent());
---

<Layout 
  title={`${siteConfig.title} · Terms of Service`} 
  description={`Terms of Service for ${siteConfig.title} application - Rules and guidelines for using our service`}
>
  <main class="min-h-screen max-w-3xl mx-auto px-4 py-8">
    <Breadcrumbs items={[{ label: "Terms of Service" }]} />
    <div class="prose prose-gray dark:prose-invert prose-sm sm:prose-base max-w-none">
      <div set:html={htmlContent} />
    </div>
  </main>
  <BackToTop client:load />
</Layout>

<style is:global>
  .prose {
    --tw-prose-headings: theme(colors.gray.900);
    --tw-prose-links: theme(colors.blue.600);
    --tw-prose-bold: theme(colors.gray.900);
    --tw-prose-invert-headings: theme(colors.white);
    --tw-prose-invert-links: theme(colors.blue.400);
    --tw-prose-invert-bold: theme(colors.white);
  }
  
  .prose {
    a {
      text-decoration: none;
      &:hover { text-decoration: underline; }
    }

    h1, h2, h3, h4, h5, h6 { color: theme(colors.gray.900); }
    p, li { color: theme(colors.gray.700); }
    strong { color: theme(colors.gray.800); }
  }

  .dark .prose {
    h1, h2, h3, h4, h5, h6 { color: theme(colors.white); }
    p, li { color: theme(colors.gray.300); }
    strong { color: theme(colors.gray.200); }
  }
</style>
